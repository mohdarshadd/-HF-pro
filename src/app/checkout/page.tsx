import CheckoutClient from "@/components/checkout/CheckoutClient";

export const metadata = {
  title: "Checkout | Hello Food",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
