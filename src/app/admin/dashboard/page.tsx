import AdminDashboard from "@/components/admin/AdminDashboard";

export const metadata = {
  title: "Dashboard | Hello Food Admin",
};

export default function DashboardPage() {
  return <AdminDashboard />;
}
